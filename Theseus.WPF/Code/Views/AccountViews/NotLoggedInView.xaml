<UserControl x:Class="Theseus.WPF.Code.Views.NotLoggedInView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:Theseus.WPF.Code.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <views:PatientLoginView  Grid.Row="0" Grid.Column="0"/>
        <views:PatientRegisterView  Grid.Row="0" Grid.Column="2"/>

        <Button Grid.Row="2" Grid.Column="2" Style="{StaticResource Generic}" FontSize="13" Command="{Binding NavigateToStaffMemberLoginRegister}" Content="Login as Staff Member" VerticalAlignment="Bottom" HorizontalAlignment="Right"/>

    </Grid>
</UserControl>
