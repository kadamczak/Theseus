<UserControl x:Class="Theseus.WPF.Code.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Theseus.WPF.Code.Views"
             mc:Ignorable="d" 
             d:DesignHeight="784.04" d:DesignWidth="1040">
    <Grid Background="WhiteSmoke" >

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 0 0 60">
            
            <Label Content="Theseus" HorizontalAlignment="Center" VerticalAlignment="Top" FontSize="80" FontFamily="Microsoft YaHei Light"  />

            <TextBlock TextWrapping="Wrap" Text="{DynamicResource TheseusHomeDescription}" MaxWidth="600" Foreground="DimGray" Margin="40 10 40 0" HorizontalAlignment="Center" FontSize="25"  TextAlignment="Center"/>

            <Button Style="{StaticResource Generic}" Margin="0 20 0 0" FontSize="19" Content="{DynamicResource ViewCredits}" Command="{Binding ViewCredits}" HorizontalAlignment="Center"/>

            <Label HorizontalAlignment="Center" FontSize="20" Margin="0 25 0 0" Content="{DynamicResource Support}"/>
            <Label HorizontalAlignment="Center" FontSize="17" Content="theseusstaff@gmail.com" />
        </StackPanel>
    </Grid>
</UserControl>
