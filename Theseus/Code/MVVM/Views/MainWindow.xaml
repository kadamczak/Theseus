<Window x:Class="Theseus.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Theseus"
        xmlns:views="clr-namespace:Theseus.Code.MVVM.Views"
        xmlns:vms="clr-namespace:Theseus.Code.MVVM.ViewModels"
        mc:Ignorable="d"
        Icon="/Resources/Icons/maze.png"
        Title="Theseus" Height="800" Width="1300">
    
    <Grid Background="WhiteSmoke">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.Resources>
            <DataTemplate DataType="{x:Type vms:HomeViewModel}">
                <views:HomeView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vms:MazeGeneratorSettingsViewModel}">
                <views:MazeGeneratorSettingsView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vms:MazeDetailViewModel}">
                <views:MazeDetailView/>
            </DataTemplate>
        </Grid.Resources>

        <views:NavigationBarView Grid.Column="0" DataContext="{Binding NavigationBarViewModel}" />
        <ContentControl Grid.Column="1" Content="{Binding CurrentViewModel}"/>
    </Grid>
    
</Window>
